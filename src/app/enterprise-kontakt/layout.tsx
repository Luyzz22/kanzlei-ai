import type { Metadata } from "next"

export const metadata: Metadata = { title: "Enterprise Demo anfragen" }

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
