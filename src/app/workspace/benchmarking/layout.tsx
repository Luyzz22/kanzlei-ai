import type { Metadata } from "next"
export const metadata: Metadata = { title: "Lieferanten-Benchmarking", description: "Risiko-Uebersicht und Konditionen-Vergleich aller Lieferanten." }
export default function Layout({ children }: { children: React.ReactNode }) { return children }
